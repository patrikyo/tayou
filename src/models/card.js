const cardClass = class Card {
  constructor(img, title, body, button, icon) {
    this.img = img;
    this.title = title;
    this.body = body;
    this.button = button;
    this.icon = icon;
  }
};

export default cardClass;
